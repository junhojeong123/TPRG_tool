name: frontend
description: "A new Flutter project for TRPG App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
    
  flutter_secure_storage: ^5.0.2
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.2
  socket_io_client: ^2.0.3+1
  http: ^0.13.5
  provider: ^6.0.5
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true


